version: '3'
services:
  couchserver:
    image: couchdb
    restart: always
    network_mode: "host"
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=password
    volumes:
        - ./dbdata:/opt/couchdb/data